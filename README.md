# Rolling Tools

A mobile-friendly dungeon master toolbox with 3D dice rolling, configurable named dice presets, a stateful card deck, draw history, shuffle, refill, and empty-deck handling.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The GitHub Pages workflow publishes `dist` from the `main` branch.
